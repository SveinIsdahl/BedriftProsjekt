{
  "CONNECTSTRING" : "postgres://tesla:123@localhost/tesla",
  "PORT" : 3000,
  "PROJECT": "tesla",
  "OPEN_TABLES": "eksemplar" ,
  "LEVEL3": [
    "select * from eksemplar",
    "select eksemplarid from eksemplar order by eksemplarid",
    "select kundeid,fornavn from  kunde order by fornavn",
    "insert into salg (dato,eksemplarid,kundeid) values ($[dato],$[eksemplarid],$[kundeid])",
    "select count(*) from eksemplar",
    "select count(*) from nyhetsbrev",
    "insert into nyhetsbrev (email) values ($[email])",
    "select kundeid,fornavn,etternavn from  kunde order by fornavn,etternavn",
    "select eksemplarid,eksemplarid,modell from eksemplar order by eksemplarid,modell"
  ],
  "LEVEL2": {
  },
  "LEVEL1": {
  }
}